# Core scanner works on stdlib alone (txt/md/html/docx-xml).
# Install these for full PDF + DOCX fidelity:
pymupdf>=1.23      # best PDF backend: colors, font sizes, positions, annotations
pypdf>=4           # fallback PDF backend (also covers PyPDF2)
python-docx>=1.1   # optional, extra DOCX styling checks
flask>=3          # local web UI (app.py)
